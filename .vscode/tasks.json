{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build client (desktop)",
			"type": "shell",
			"command": "cmake --build build-release --target client --config Release",
			"problemMatcher": [
				"$msCompile"
			],
			"group": "build"
		}
	]
}